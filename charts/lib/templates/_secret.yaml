{{- define "lib.secret" -}}
{{- range .Values.secrets -}}
{{- $runsOnList := required "A valid runsOnList is required!" .runsOnList -}}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ $.Release.Name }}-{{ .name }}
  labels:
    {{- if .runsOn }}
    runs-on: {{ .runsOn }}
    {{- end }}
    {{- range .runsOnList }}
    runs-on-{{ . }}: "true"
    {{- end }}
    instance: {{ $.Release.Name }}-{{ .name }}
    component: {{ .component | default "sealedSecret" }}
    part-of: {{ $.Release.Name }}
spec:
  encryptedData:
{{ toYaml .data | indent 4 }}
  template:
    metadata:
      name: {{ $.Release.Name }}-{{ .name }}
      labels:
        {{- if .runsOn }}
        runs-on: {{ .runsOn }}
        {{- end }}
        {{- range .runsOnList }}
        runs-on-{{ . }}: "true"
        {{- end }}
        instance: {{ $.Release.Name }}-{{ .name }}
        component: {{ .component | default "secret" }}
        part-of: {{ $.Release.Name }}
---
{{- end -}}
{{- end -}}