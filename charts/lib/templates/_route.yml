{{- define "lib.route" -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
namespace: traefik
metadata:
  name: {{ .Values.name }}-{{ .Values.env }}-http
spec:
  parentRefs:
    - name: traefik
      kind: Gateway
  hostnames:
    - {{ .Values.host }}
  rules:
     - matches:
        - path:
            type: PathPrefix
            value: /
       backendRefs:
        - name: {{ .Values.name }}-{{ .Values.env }}-service-external
          port: 80
{{- end -}}