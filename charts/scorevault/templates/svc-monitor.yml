apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Values.name }}-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: {{ .Values.name }}-server
  endpoints:
    - port: web
